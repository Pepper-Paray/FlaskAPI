# Simple Flask API

This project is a small Flask backend that connects to a SQLite database and stores student records. The API includes two working routes: one that reads all students from the database and one that creates a new student. It uses a very simple data model with just an `id` and a `name`, making it easy to understand and test.
